default sistem : /usr/share/applications/mimeinfo.cache 

1. git reset --soft HEAD~1           ###################
2. git push origin +main --force     #github yorum silme
3. git add .
4. git commit -m "uwu"


cd / mv / cp / rm / ls / pwd / 
find / -name file
whereis 

killall dunst;notify-send foo - dunst sıfırlama


bottle game > mono-gecko-allfont

steam
PROTON_NO_D3D11=1  LD_PRELOAD=$LD_PRELOAD:/usr/\$LIB/libgamemodeauto.so %command%

sudo pacman -Sy rustup
rustup default stable


set url.searchengines '{"DEFAULT": "https://google.com/?q={}"}'
qutebrowser defaul search engine